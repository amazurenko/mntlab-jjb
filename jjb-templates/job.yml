- job-template:
      name: 'MNTLAB-{student}-test-build-job'
      parameters:
          - dynamic-choice:
                name: BRANCH_NAME
                script: "['akhurhin', 'master']"

- project:
        name: test1
        student: akhurhin
        jobs:
            - 'MNTLAB-{student}-test-build-job'

